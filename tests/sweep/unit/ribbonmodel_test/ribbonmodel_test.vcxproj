<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ItemGroup Label="ProjectConfigurations">
    <ProjectConfiguration Include="Release|x64">
      <Configuration>Release</Configuration>
      <Platform>x64</Platform>
    </ProjectConfiguration>
  </ItemGroup>
  <PropertyGroup Label="Globals">
    <VCProjectVersion>17.0</VCProjectVersion>
    <ProjectGuid>{4D1C9E00-0001-4A00-9000-00000000D00B}</ProjectGuid>
    <RootNamespace>ribbonmodel_test</RootNamespace>
    <Keyword>Win32Proj</Keyword>
    <WindowsTargetPlatformVersion>10.0</WindowsTargetPlatformVersion>
  </PropertyGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.Default.props" />
  <PropertyGroup Label="Configuration">
    <ConfigurationType>Application</ConfigurationType>
    <UseDebugLibraries>false</UseDebugLibraries>
    <PlatformToolset>v145</PlatformToolset>
    <CharacterSet>Unicode</CharacterSet>
  </PropertyGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.props" />
  <Import Project="..\UnitTest.props" />
  <ItemGroup>
    <ClCompile Include="ribbonmodel_test.cpp" />
    <!-- The shipping sources, so what passes is the code that ships. The model
         reaches only core::modes, which is why the ribbon's decisions can be
         tested with no Excel, no COM and no add-in. -->
    <ClCompile Include="$(XRayRoot)src\ui\ribbonmodel.cpp" />
    <ClCompile Include="$(XRayRoot)src\core\tracemodes.cpp" />
    <ClCompile Include="$(XRayRoot)src\core\notify.cpp" />
    <!-- The buffer size box parses and formats with the SAME code the command
         surface uses, so the two cannot drift. -->
    <ClCompile Include="$(XRayRoot)src\app\paramparse.cpp" />
  </ItemGroup>
  <Import Project="$(VCTargetsPath)\Microsoft.Cpp.targets" />
</Project>
